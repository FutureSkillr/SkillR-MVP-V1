# SkillR — Environment Variables
# Copy to .env.local and fill in values.
# NEVER commit .env.local — it is gitignored.

# ── Go Backend (required) ─────────────────────────────────────────────
DATABASE_URL=postgres://skillr:localdev@localhost:5432/skillr?sslmode=disable

# ── Go Backend (optional — set these for `make go-dev` / `make dev-all`) ──
# These connect the local Go backend to Docker services started by `make services-up`.
REDIS_URL=redis://localhost:6379/0
RUN_MIGRATIONS=true
STATIC_DIR=../frontend/dist
MIGRATIONS_PATH=./migrations

# ── Gemini API (Layer 1: AI Proxy) ─────────────────────────────────────
GEMINI_API_KEY=

# ── Firebase Config (Layer 2: Runtime Injection) ───────────────────────
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# ── Admin Seed (leave blank to skip admin seeding) ──────────────────
ADMIN_SEED_EMAIL=
ADMIN_SEED_PASSWORD=

# ── CORS Origins (comma-separated) ──────────────────────────────────
# ALLOWED_ORIGINS=https://your-domain.run.app

# ── Warteraum / Capacity (FR-062) ──────────────────────────────────
# QUEUE_ENABLED=false
# MAX_CONCURRENT_GEMINI_SESSIONS=10
# AVG_SESSION_DURATION_MS=180000

# ── Meta Pixel (FR-064) ──────────────────────────────────────────
# META_PIXEL_ID=

# ── Flood Detection (FR-065) ─────────────────────────────────────
# FLOOD_THRESHOLD=50

# ── Health Check (FR-067) ────────────────────────────────────────────
# Shared secret for /api/health/detailed endpoint. Generate with: openssl rand -hex 16
# HEALTH_CHECK_TOKEN=

# ── GCP / VertexAI ───────────────────────────────────────────────────
# GCP_PROJECT_ID=
# GCP_REGION=europe-west3
# GCP_TTS_REGION=europe-west1  # TTS/STT region (Gemini TTS supported: europe-west1, europe-west4, europe-central2)
# CLOUD_RUN_SERVICE=skillr

# ── GCP Credentials (FR-069) ────────────────────────────────────────
# Local dev: path to service account key JSON (stored in gitignored credentials/)
# Cloud Run: mounted from Secret Manager via --set-secrets (see scripts/setup-secrets.sh)
# GOOGLE_APPLICATION_CREDENTIALS=credentials/vertexai-sa.json

# ── Honeycomb API (Lernreise Tracking — FR-072) ──────────────────
# HONEYCOMB_URL=https://honeycomb.example.com
# HONEYCOMB_API_KEY=

# ── Memory Service (User Context Sync — FR-073) ──────────────────
# MEMORY_SERVICE_URL=https://memory.example.com
# MEMORY_SERVICE_API_KEY=

# ── Solid Pod (Data Sovereignty — FR-076) ────────────────────────
# For local dev with `make services-up`:
SOLID_POD_URL=http://localhost:3003
SOLID_POD_ENABLED=true

# ── Admin Seed (auto-creates admin user on first startup) ────────
ADMIN_SEED_EMAIL=admin@skillr.local
ADMIN_SEED_PASSWORD=Admin1local
